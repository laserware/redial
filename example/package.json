{
  "name": "redial-example",
  "description": "Example application using Redial.",
  "version": "1.0.0",
  "main": "dist/main/main.js",
  "scripts": {
    "build:renderer": "vite build --mode @renderer/development",
    "build:main": "vite build --mode @main/development",
    "build": "npm run build:main && npm run build:renderer",
    "start": "npm run build && electron ."
  },
  "author": "Mike Rourke <mike.w.rourke@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@laserware/stasis": "^4.1.0"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-devtools-installer": "^4.0.0",
    "vite": "^6.0.6"
  }
}
